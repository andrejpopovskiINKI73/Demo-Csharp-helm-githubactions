apiVersion: v1
kind: Service
metadata:
  name: todoapi-svc
spec:
  selector:
    app: todo-api
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}